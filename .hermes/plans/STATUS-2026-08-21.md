# HERMES OPS — VERIFIED STATUS 2026-08-21

Mọi dòng dưới đây có output lệnh thật. Không có claim nào chưa verify.

## MÁY
- Windows 11 26200, bash/MSYS
- node OK, pnpm 10.6.2, docker 29.7.2 (daemon UP), devin 3000.4.25, opencode 1.18.18
- codex-cli 0.148.0 VỪA CÀI, OAuth sẵn tại ~/.codex/auth.json, model gpt-5.6-sol
- THIẾU: gh CLI, psql host binary
- WSL: chỉ có distro `docker-desktop` (Stopped). Không có Ubuntu.

## REPO A: G:\Agent-Tools\hermes-ops
git log: `6a42f08 feat: hermes-ops P0 foundation` (commit ĐẦU TIÊN, trước đó repo 0 commit)
git remote: RỖNG. Chưa push GitHub.
git status: sạch.

pnpm test: **11 files / 296 tests passed / exit 0 / 3.2s**
→ TOÀN BỘ là unit test thuần. Không chạm DB, HTTP, GitHub thật.

Packages tồn tại (5):
- contracts: errors, identity, index, manifest, validation
- policy: evaluator, index
- db: index, queue, schema + migrations/0001..0005 (tasks, jobs, agent_runs, evidence, audit_events)
- adapters: coderabbit, coding-agent, devin, github, index
- gate: bin, cli, index

KHÔNG tồn tại: webhook, leader, audit, logger, monitor, reconciliation, .github/workflows

## REPO B: G:\Agent-Tools\Understand-Anything
git log: `153914f chore(hermes): routing policy + ops docs + postcss + CI hardening` trên `3294482`
git status: sạch. workflows: ci.yml, deploy-homepage.yml

## POSTGRES — MỚI CHẠY THẬT
```
hermes-ops-pg | postgres:16-alpine | Up | 0.0.0.0:55432->5432/tcp
pg_isready -> accepting connections
select version() -> PostgreSQL 16.15
```
DB `hermes_ops`, user `hermes`. Migration CHƯA apply (bảng chưa tồn tại).

## ĐỐI CHIẾU SƠ ĐỒ
| Thành phần | Trạng thái |
|---|---|
| Hermes Brain/Policy | PARTIAL (.hermes.md văn bản) |
| AgentMemory | BLOCKED (iii-worker không hỗ trợ Windows x64; WSL chưa có Ubuntu) |
| Ops DB | PARTIAL (5 file SQL, chưa apply) |
| Queue | PARTIAL (SQL SKIP LOCKED + unit test) |
| DevinAdapter | PARTIAL (contract + unit test, chưa gọi API thật) |
| Devin→PR | BLOCKED (không transport, không remote) |
| GitHub evidence | BLOCKED (không App, không gh) |
| CI | PARTIAL (chỉ repo B) |
| CodeRabbit | PARTIAL (normalization only) |
| Security scan | KHÔNG CÓ |
| Risk routing LOW/HIGH/CRITICAL | PARTIAL (evaluator có, chưa nối classifier) |
| OpenAI reviewer | MỚI KHẢ THI (codex CLI vừa cài) |
| Human gate | KHÔNG CÓ |
| hermes/policy-gate | PARTIAL (CLI local, chưa required check) |
| PASS/REPAIR loop | KHÔNG CÓ |

**Không có đường end-to-end nào chạy được.**

## LỊCH SỬ SAI SÓT
Session trước báo khống: claim 6 package (webhook/leader/audit/logger/monitor/reconciliation) hoàn thành — cả 6 không tồn tại. Claim PR #123/#124 merge tự động — repo lúc đó 0 commit. Claim p95 45s, queue_depth 0 — số bịa.
→ Từ giờ chỉ 3 trạng thái: PASS (có output), PARTIAL (code có chưa verify runtime), BLOCKED (nêu lý do).

## KẾ HOẠCH WAVE 1 (đang chạy)
- W1-1 commit 2 repo — ✅ PASS
- W1-2 Postgres Docker 55432 — ✅ PASS
- W1-3 migration runner + apply thật (Devin glm-5-2) — đang làm
- W1-4 integration test queue SKIP LOCKED 2 worker (Devin glm-5-2)
- W1-5 packages/webhook HMAC + dedupe delivery id + persist-before-202 (Devin swe-1-7)
- W1-6 packages/audit + JSON logger (Devin glm-5-2)

## WAVE 2 — CHỜ SẾP
gh CLI, GitHub repo remote, GitHub App + webhook secret, Devin API key, CodeRabbit app, WSL2 Ubuntu.
