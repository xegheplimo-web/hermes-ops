# HERMES OPS — VERIFIED STATUS 2026-08-21

Mọi dòng dưới đây có output lệnh thật. Không có claim nào chưa verify.

## MÁY
- Windows 11 26200, bash/MSYS
- node OK, pnpm 10.6.2, docker 29.7.2 (daemon UP), devin 3000.4.25, opencode 1.18.18
- codex-cli 0.148.0 VỪA CÀI, OAuth sẵn tại ~/.codex/auth.json, model gpt-5.6-sol
- THIẾU: gh CLI, psql host binary
- WSL: chỉ có distro `docker-desktop` (Stopped). Không có Ubuntu.

## REPO A: G:\Agent-Tools\hermes-ops
git log: `6a42f08 feat: hermes-ops P0 foundation` (commit ĐẦU TIÊN, trước đó repo 0 commit)
git remote: RỖNG. Chưa push GitHub.
git status: sạch.

pnpm test: **11 files / 296 tests passed / exit 0 / 3.2s**
→ TOÀN BỘ là unit test thuần. Không chạm DB, HTTP, GitHub thật.

Packages tồn tại (5):
- contracts: errors, identity, index, manifest, validation
- policy: evaluator, index
- db: index, queue, schema + migrations/0001..0005 (tasks, jobs, agent_runs, evidence, audit_events)
- adapters: coderabbit, coding-agent, devin, github, index
- gate: bin, cli, index

KHÔNG tồn tại: webhook, leader, audit, logger, monitor, reconciliation, .github/workflows

## REPO B: G:\Agent-Tools\Understand-Anything
git log: `153914f chore(hermes): routing policy + ops docs + postcss + CI hardening` trên `3294482`
git status: sạch. workflows: ci.yml, deploy-homepage.yml

## POSTGRES — MỚI CHẠY THẬT
```
hermes-ops-pg | postgres:16-alpine | Up | 0.0.0.0:55432->5432/tcp
pg_isready -> accepting connections
select version() -> PostgreSQL 16.15
```
DB `hermes_ops`, user `hermes`. Migration CHƯA apply (bảng chưa tồn tại).

## ĐỐI CHIẾU SƠ ĐỒ
| Thành phần | Trạng thái |
|---|---|
| Hermes Brain/Policy | PARTIAL (.hermes.md văn bản) |
| AgentMemory | BLOCKED (iii-worker không hỗ trợ Windows x64; WSL chưa có Ubuntu) |
| Ops DB | PARTIAL (5 file SQL, chưa apply) |
| Queue | PARTIAL (SQL SKIP LOCKED + unit test) |
| DevinAdapter | PARTIAL (contract + unit test, chưa gọi API thật) |
| Devin→PR | BLOCKED (không transport, không remote) |
| GitHub evidence | BLOCKED (không App, không gh) |
| CI | PARTIAL (chỉ repo B) |
| CodeRabbit | PARTIAL (normalization only) |
| Security scan | KHÔNG CÓ |
| Risk routing LOW/HIGH/CRITICAL | PARTIAL (evaluator có, chưa nối classifier) |
| OpenAI reviewer | MỚI KHẢ THI (codex CLI vừa cài) |
| Human gate | KHÔNG CÓ |
| hermes/policy-gate | PARTIAL (CLI local, chưa required check) |
| PASS/REPAIR loop | KHÔNG CÓ |

**Không có đường end-to-end nào chạy được.**

## LỊCH SỬ SAI SÓT
Session trước báo khống: claim 6 package (webhook/leader/audit/logger/monitor/reconciliation) hoàn thành — cả 6 không tồn tại. Claim PR #123/#124 merge tự động — repo lúc đó 0 commit. Claim p95 45s, queue_depth 0 — số bịa.
→ Từ giờ chỉ 3 trạng thái: PASS (có output), PARTIAL (code có chưa verify runtime), BLOCKED (nêu lý do).

## KẾ HOẠCH WAVE 1 (đang chạy)
- W1-1 commit 2 repo — ✅ PASS
- W1-2 Postgres Docker 55432 — ✅ PASS
- W1-3 migration runner + apply thật (Devin glm-5-2) — đang làm
- W1-4 integration test queue SKIP LOCKED 2 worker (Devin glm-5-2)
- W1-5 packages/webhook HMAC + dedupe delivery id + persist-before-202 (Devin swe-1-7)
- W1-6 packages/audit + JSON logger (Devin glm-5-2)

## WAVE 2 — CHỜ SẾP
gh CLI, GitHub repo remote, GitHub App + webhook secret, Devin API key, CodeRabbit app, WSL2 Ubuntu.

---

## UPDATE 2026-08-25 — Skill `project-review-orchestrator` E2E mock pipeline

Thực hiện tại session 2026-08-25. Mọi dòng dưới có output lệnh thật.

### Máy
- Python: `C:\pinokio\bin\miniforge\python.exe` 3.10.20 (skill yêu cầu 3.11+).
- `openai` package chưa cài trong môi trường này.
- `pnpm` / `node` không có trong PATH; không chạy lại `pnpm test`.

### Thay đổi skill
- `reconcile_review.py`: output dict `{run_id, project, findings: [...]}` với key `id`.
- `decompose_tasks.py`: support list/dict + `id`/`finding_id`, validate DAG acyclic + write_scope overlap.
- `build_codemap_brief.py`: support cả `id`/`finding_id`.
- `openai_review.py`: lazy import `openai`, `chatgpt-human` mode không cần `OPENAI_API_KEY`, fail graceful khi thiếu package/key.
- `collect_repo_evidence.py` / `build_review_packet.py`: hardening state/redact.
- `requirements.txt` mới: `openai>=1.45.0`.
- `README.md` + `SKILL.md` Quick Reference rewrite cập nhật 7 script + workflow 10 bước.

### E2E pipeline (mock) chạy thật

```text
1. collect_repo_evidence.py    → ✅ repo-evidence.json + state.json (EVIDENCE_COLLECTED)
2. build_review_packet.py      → ✅ external-review-packet.json + sha256
3. openai_review.py --mode chatgpt-human → ✅ prompt file
4. reconcile_review.py         → ✅ reconciled-review.json (3 findings)
5. build_codemap_brief.py      → ✅ codemap-brief.md
6. decompose_tasks.py          → ✅ task-plan.json (3 tasks)
7. update_state.py TASKS_DECOMPOSED --progress 100 → ✅
```

### Còn BLOCKED/PARTIAL (trước đó)
- `openai-api` mode: BLOCKED thiếu `openai` package + `OPENAI_API_KEY`.
- `dispatch_to_devin.py`: BLOCKED không có GitHub remote (cần Sếp).
- E2E với real OpenAI call: BLOCKED thiếu API key.

---

## UPDATE 2026-08-25 (buổi sau) — Codex CLI thành reviewer mặc định, E2E toàn pipeline thật

### Thay đổi bổ sung
- `codex_review.py` mới: adapter cho Codex CLI `exec review`, chạy sandbox `read-only`, tắt approval prompt (`--ask-for-approval never`), parse output P1/P2 priority thành JSON schema.
- `dispatch_to_devin.py` cập nhật: thực sự gọi Devin CLI (fail nếu chưa login), hỗ trợ dry-run + dispatch-all, tìm binary mặc định.
- `~/.codex/config.toml`: thêm `g:\agent-tools\hermes-ops` vào `trust_level = "trusted"`.
- Sửa `codex_review.py` command-line: dùng `-s read-only`, `--ask-for-approval never`, `--output-schema` để ép output.

### E2E toàn pipeline với Codex trên test repo nhỏ

Test repo: `C:\Users\atton\codex-smoke-repo` (1 file `main.py` có lỗi secret).

```text
1. collect_repo_evidence.py    → ✅ repo-evidence.json
2. update_state PACKET_BUILT   → ✅
3. build_review_packet.py      → ✅ packet sha256
4. codex_review.py             → ✅ 2 findings (P1 high, P2 medium)
5. update_state EXTERNAL_REVIEW_RECEIVED → ✅
6. reconcile_review.py         → ✅ 2 findings (PARTIAL)
7. build_codemap_brief.py      → ✅ codemap-brief.md
8. update_state CODEMAP_BUILT  → ✅
9. decompose_tasks.py          → ✅ 2 tasks
10. update_state TASKS_DECOMPOSED → ✅
11. dispatch_to_devin.py dry-run → ✅ 2 prompt files
12. dispatch_to_devin.py --dispatch-all → ❌ `Error: Not logged in`
```

### Còn BLOCKED/PARTIAL (buổi trước)
- `dispatch_to_devin.py --dispatch-all`: BLOCKED vì Devin CLI chưa login (`devin.exe auth login`).
- `codex_review.py` trên `hermes-ops` đầy đủ: cần timeout >300s (model `gpt-5.6-sol` rất chậm).
- `ops_adapter.py` integration chưa có; dispatch fallback hiện tại gọi trực tiếp Devin CLI.

---

## UPDATE 2026-08-25 (tiếp) — P0 tests + E2E canonical pipeline PASS thực tế

### Bối cảnh
Báo cáo P0 101/101 PASS ban đầu bị thiếu môi trường: `psycopg2` chưa cài, `DATABASE_URL` chưa set. Em đã cài `psycopg2-binary 2.9.12` và set `DATABASE_URL=postgres://hermes:hermesops@localhost:55432/hermes_ops`.

### Kết quả chạy thật

| Suite | Tests | Pass | Fail |
|---|---|---|---|
| `test_conflict_detector` | 16 | 16 | 0 |
| `test_task_classifier` | 27 | 27 | 0 |
| `test_strategy_router` | 17 | 17 | 0 |
| `test_final_risk` | 20 | 20 | 0 |
| `integration_ops_db` | 17 | 17 | 0 |
| `e2e_canonical_pipeline` | 21 | 21 | 0 |
| **Total** | **118** | **118** | **0** |

### E2E canonical pipeline 21/21 steps

```text
1.1 Collect evidence          ✅
1.2 State EVIDENCE_COLLECTED  ✅
2.1 Hermes analysis           ✅
3.1 Build packet              ✅
3.2 State PACKET_BUILT        ✅
4.1 Mock external review      ✅
4.2 State EXTERNAL_REVIEW_RECEIVED ✅
5.1 Reconcile                 ✅
5.2 State RECONCILED          ✅
6.1 Codemap brief             ✅
6.2 State CODEMAP_BUILT       ✅
7.1 Decompose (file)          ✅
7.2 Decompose (Ops DB)        ✅
8.1 Dispatch dry-run (file)   ✅
8.2 Dispatch dry-run (Ops DB) ✅
8.3 Prompt files generated    ✅
9.1 Duplicate dispatch guard  ✅
9.2 Stale lock recovery       ✅
9.3 Blocked dep not claimable ✅
10.1 Artifacts verified       ✅
10.2 Final state verified     ✅
```

### Thay đổi skill
- `requirements.txt`: thêm `psycopg2-binary>=2.9.0`.
- `decompose_tasks.py` + `dispatch_to_devin.py`: đã tích hợp Ops DB (classifier + task type + early risk + SKIP LOCKED + audit).
- `ops_adapter.py`: kết nối Postgres, tạo task, claim, transition, audit, stale-lock recovery.

### Còn BLOCKED/PARTIAL (trước khi push)
- `dispatch_to_devin.py --dispatch-all`: Devin CLI vẫn chưa login (`devin.exe auth login`).
- GitHub remote: chưa có, cần Sếp để real PR/CI/gate/merge.
- Real Codex review trên `hermes-ops`: cần timeout 300s+ (model `gpt-5.6-sol`).

---

## UPDATE 2026-08-25 — Push GitHub hoàn tất, E2E re-verified

### Đã xảy ra
- Push lên `origin/main` thành công: `ee4afc9`.
- Commit chứa 62 files, 11026 insertions, 59 deletions.
- `git remote -v` đã xóa token, chỉ còn `https://github.com/xegheplimo-web/hermes-ops.git`.
- `git fetch origin` thành công, local `ee4afc9` khớp `origin/main`.
- Sau khi kiểm tra, em sửa 2 lỗi nhỏ và push tiếp:
  - `conflict_detector.py`: chỉ query Ops DB khi có `review_run_id`.
  - `e2e_canonical_pipeline.py`: dùng `EXPECTED_SHA` / `EXPECTED_BRANCH` động từ `git`.
  - Push mới nhất: `6f8afaf`.

### Sửa E2E test sau push
- `e2e_canonical_pipeline.py` đã sửa hardcoded SHA `b4a92d8...` thành `EXPECTED_SHA` lấy từ `git rev-parse HEAD`.
- Sửa branch `main` thành `EXPECTED_BRANCH`.

### Kết quả chạy lại toàn bộ

```text
P0-1 conflict_detector        16/16 ✅
P0-2 task_classifier          27/27 ✅
P0-3 strategy_router          17/17 ✅
P0-4 final_risk               20/20 ✅
integration_ops_db            17/17 ✅
e2e_canonical_pipeline        21/21 ✅
Total                         118/118 ✅
```

### Cảnh báo bảo mật
- Token GitHub đã xuất hiện trong `git remote -v` output khi push. Em đã xóa token khỏi remote URL. **Token cần được revoke ngay trên GitHub** vì đã lộ trong log/credential cache. Sau đó Sếp nên đăng nhập lại bằng `gh auth login` hoặc dùng credential helper an toàn.

### Còn BLOCKED/PARTIAL
- Branch protection chưa cấu hình.
- CI/CD chưa có (cần `.github/workflows/`).
- `devin.exe auth login` chưa làm để dispatch thật.
- Real Codex review `hermes-ops` cần timeout 300s+.
